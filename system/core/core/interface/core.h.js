'use strict';

module.exports = class coreFunctionalities {

    constructor() { };

    /**
     * At some point, SHPS has to shutdown. All modules will have to die then.
     */
    _die() { throw 'Not implemented'; };
};
